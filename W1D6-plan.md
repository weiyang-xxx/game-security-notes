# 明日学习计划 · W1 第六天（2026-09-04 周五）

> 来源：游戏安全工程师培养计划 v3.2 + S1 执行方案 W1 逐日任务卡（§5，D6–7 缓冲）+ 协作机制（方案 B，已选定）
> 性质：**预生成，明天照着做**。验收看证据，不看感觉。
> 本日定位：缓冲日——W1 计划里明写的 40% 补课额度的一部分（S1 §0.2）。把前 5 天没吃透的回锅，不进新内容。
> **AI 写码占比 ≤40% 红线（计划 §5.7 / S1 核心原则）前置声明**：今天只是回锅 + 写笔记，不算写码；但记住——核心模块（读取器 / PE 解析 / AOB）第一次必须你自己手写，导师只给骨架（含 `TODO(learner)`），今天不碰核心实现。

---

## 0. 今天一句话目标
回锅 D2–D5 没吃透的点，写出第一周公开笔记初稿（≥1 篇）。今天不进新周次内容。

---

## 1. 主任务 · 缓冲 + 回锅（~3 小时，按需）

1. **自查 D2–D5 还有哪些裂缝**（对着清单过，不要凭感觉说"都懂了"）：
   - `main.cpp` 三个刻意设计是否真看懂？
     - 递归探栈输出：局部变量地址是否逐层变小（这是"栈向下"的**证据**，不是断言）
     - 堆单调性自检：连续 `new` 地址是否严格递增——**答"否"是正常的**（空闲块复用 + 大小分级 bin），你要会解释**为什么**，不是记结论
     - `.data`（`&g_msg_ptr` 指针本身）vs `.rdata`（它指向的字符串字面量）是两个不同地址，别再混
   - 3 句解释能否脱稿说清？尤其 **"绝对高低由 ASLR 决定，我不记结论"** 这一条
   - D4 费曼卡住的点是否补上了？
   - D5 的 GitHub 仓库 / push / 博客站是否真通？（MEMORY.md 记录：D5=2026-09-03 已完成第一版 push，博客站 GitHub Pages 已建）
2. 哪条还虚 → 今天专攻那条。可以重编译、重讲、重画，直到不虚。
3. **写第一周公开笔记初稿（≥1 篇）**：主题如"W1 内存四区 + 我踩的 ASLR 错觉"，用大白话写，周末发博客。

**验收硬门槛（缺一不算完成）**：
- [ ] 明确写出"本周还有哪 N 处没吃透" + 今日补了哪些（落到日报里）
- [ ] 第一周公开笔记初稿 ≥1 篇（存 `progress/韦扬/notes/`，见 §3）

## 1.5 两个计划生成时漏列的必补尾项（今日硬交付）

> D5 日报「明日第一件事」已点名这两件属于 D6，但上面的自动生成计划没列。今天必须显式补上，否则 D4 的 🟡 和 D5 的 git 弱项会直接拖进 W2。

1. **补 D4 卡点表第四栏「知识重述」（关掉唯一 🟡）**
   - 打开 `progress/韦扬/notes/d4-stuck.md`，在第四栏补写一段话（**自己的话**）。
   - 三要素齐全：**谁**定下局部变量地址（编译器）/ **何时**定（编译期，按编译键那一刻就固定，不是运行期也不是装入时）/ **为何 ASLR 洗不掉**（房间号是相对帧底的固定量、写死在机器指令里；ASLR 只随机化绝对基址，不碰指令里的相对常数）。
   - 硬约束：**禁「偏移」二字、禁抄原句**。写「房间号」「固定间隔」都行，就是不能写「偏移」。
   - 验收：三个月后的你读这段能一秒接上 = 达标。
2. **git 三连坑回锅（D5 弱项，费曼自检，不重推）**
   - 不开新仓库、不重推（仓库已对）。今天把正确流程写一遍 + 说清每坑根因：
     - 坑1 占位符未换 → `git remote set-url origin https://github.com/weiyang-xxx/game-security-notes.git`
     - 坑2 只 add 没 commit → `git commit -m "..."` 才有分支对象（否则报 `src refspec master does not match any`）
     - 坑3 站错目录推空仓库 → 先 `pwd` 确认在项目根（含 `.git`）；若 HOME 被误 init 成仓库，`rm -rf ~/.git` 删掉（只删 git 元数据，不动个人文件）
   - 写一份「git 正确 5 步」到日报或 notes，能不看资料一次写对 = 🟢。

---

## 2. 配套纪律（严师铁律）

- **缓冲不是偷懒**：是计划内的补课额度（S1 §0.2 给 40%）。用掉不丢人，硬推才丢人。零基础前 12 周卡壳高发（§0.2 点名 W1–W4），今天就是把裂缝在扩大前焊上。
- **6+1 节奏**：今天 = W1 第 6 个学习日，明天（D7 周六）是最后一个缓冲日，后天（周日 9/6）是固定周会。休息日是排期的一部分，但缓冲日不是休息日。
- **AI 写码占比 ≤ 40% 红线**（前 3 月硬约束）：今天零代码增量；若回锅时想"让 AI 帮我把笔记/代码补完"，占比必须守住，核心理解必须自己来。
- **日报（方案 B）**：收工发 IM 群三行 + 三色灯，≤3 分钟。格式：
  ```
  【D6 ｜ W1 ｜ 🟢/🟡/🔴】
  今天要回锅：D2–D5 还虚的点 + 写第一周笔记初稿
  跑通了没：补了 [具体 N 处] / 卡在 [具体一步]
  明天第一件事：手画内存四区图 + W1 weekly.md 归档 + 笔记定稿发博客
  ```
  最后一句"明天第一件事"极重要——把明天的启动成本降到 0。

---

## 3. 今日必须落盘的东西（含个人进度隔离硬规则）

**个人进度隔离（硬规则，MEMORY.md 载明）**：本共享日计划 `plan-*.md` 留 `memory/` 根，两人同看，**不算个人进度**。你（韦扬，learner #1）的每一项个人产出**必须**存到自己独立目录，**禁止跨成员混写、禁止合并到同一文件、禁止互相覆盖**。其他成员加入后按各自用户名建独立目录。

| 产出类型 | 落盘路径（韦扬） |
|---|---|
| 日报 + 验收证据（截图/命令输出/回锅清单/笔记链接） | `.workbuddy/memory/progress/韦扬/daily/2026-09-04.md` |
| 周报（本周日 9/6 周会前归档，契约 7.9 格式） | `.workbuddy/memory/progress/韦扬/weekly/weekly.md` |
| 公开笔记初稿 | `.workbuddy/memory/progress/韦扬/notes/` |

- [ ] ✅ 回锅清单（虚的点 + 今日补了哪些）→ 写进 `daily/2026-09-04.md`
- [ ] ✅ **D4 卡点表第四栏「知识重述」补写** → 写进 `progress/韦扬/notes/d4-stuck.md` 第四栏（关掉 D4 唯一 🟡）
- [ ] ✅ **git 三连坑回锅：正确 5 步 + 三坑根因** → 写进 `daily/2026-09-04.md` 或 `notes/`
- [ ] ✅ 第一周公开笔记初稿 ≥1 篇 → 存 `progress/韦扬/notes/`
- [ ] ✅ 日报三行 + 三色灯发 IM 群
- [ ] ⏸️ GitHub 仓库：D5 已 push 第一版，今天若动了笔记/代码，记得 `add/commit/push` 保持连续链（仓库须 Public）

---

## 4. 明天第一件事（D7 · 周六 9/5）

- **手画内存四区图（不看资料，≤5 分钟）**：从低地址到高地址画一遍（代码/.rdata/.data/.bss/堆/栈/模块/TEB-PEB），并指出栈指针往哪个方向动。这是 W1 退出标准①，也是周日周会抽问题目。
- **W1 weekly.md 归档**：按契约 7.9 格式写进 `progress/韦扬/weekly/weekly.md`（产出证据 / 一句话总结 / 卡在哪）。
- **第一周公开笔记定稿**，push 到 GitHub Pages 博客（`https://weiyang-xxx.github.io/game-security-notes/`，D5 已建）。
- 准备周日 09:00 周会抽问：**3 分钟口述**——进程/模块/节区/IAT/PEB 各是什么 + 指针链解算数据流（画 ASCII 图）。

---

## 5. 常见坑

- ❌ 缓冲日拿来刷手机 → 缓冲是补课，不是休息日（休息日排在另一块，别偷换概念）。
- ❌ 假装"都懂了"跳过回锅 → 周末抽问一考就露馅，那时补成本更高（且直接拖 W2 对齐/padding 的地基）。
- ❌ 重编译 `main.cpp` 时切到 Release / /O2 → 变量被优化进寄存器，"地址"根本不存在，结论全错。W1 一律 Debug / -O0。
- ⚠️ 把"ASLR 每次结果不同"当成自己写错了 → 那是正常的，谁高谁低每次可能不同，只能靠实测与范围判断。

---

## 6. 严师一句话

你以为"差不多懂了"的地方，往往就是下周（W2 对齐/padding）会炸的地方——今天不留它。

零基础前 12 周卡壳高发，缓冲是计划给的**补课额度**，不是奖励也不是惩罚。把 D2–D5 的裂缝在周日周会前焊死，周一进 W2 才不会被地基拖死。连续链从 D1 第一条日报起算，绿格别断。

---

> 红线提醒（计划 §1.1）：靶场只限自写程序 / Lyra / CrackMe / 公开样本（静态）/ CTF / SRC 授权环境。**任何「去某在线游戏里试试」的想法，一律不做。** 今天只是回锅 + 写笔记，碰不到红线，但先刻进脑子。
