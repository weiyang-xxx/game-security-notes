# D2 日报 ｜ W2 ｜ 2026-09-08 周二

【D2 ｜ W2 ｜ 🟢】

## 今天任务：手算 3 个结构体大小与成员偏移（纯纸算，不写码）

### 手算稿（4 步法：列对齐值 → 逐个算偏移 → 算结束地址 → 总大小取整到最大对齐值）

**A `{char a; int b; char c;}`**
- a 对齐1 @0，占0..1，用到1
- b 对齐4 → ceil(1,4)=4 @4，占4..8，用到8
- c 对齐1 @8，占8..9，用到9
- 结束9，最大对齐4 → size=ceil(9,4)=**12**
- **a@0　b@4　c@8　size=12**

**B `{int b; char a; char c;}`**（与A同成员换序）
- b 对齐4 @0，占0..4，用到4
- a 对齐1 @4，占4..5，用到5
- c 对齐1 @5，占5..6，用到6
- 结束6，最大对齐4 → size=ceil(6,4)=**8**
- **b@0　a@4　c@5　size=8**

**C `{char buf[100]; double d;}`**（复习 09-07 导师自纠错）
- buf 对齐1（元素char）@0，占0..100，用到100
- d 对齐8 → ceil(100,8)=**104** @104，占104..112，用到112
- 结束112，最大对齐8，112已是8倍数 → size=**112**
- **buf@0　d@104　size=112**　⚠️ 104 是 d 的偏移，不是总大小

### 验收点
- ✅ A/B/C 总大小 = 12 / 8 / 112，全对
- ✅ C 的 d@104（偏移）≠ 112（总大小）—— 没把昨天的"偏移当大小"错再犯
- ✅ 能解释 A(12)≠B(8) 但成员相同：换序改变 padding 落点 → 浪费量变（A 在 char 后补 3 字节缝给 int；B 把两个 char 塞进 int 留的空当，padding 从 3 字节降到 1 字节）

## 明天第一件事（D3）
- 写 `offsetof` / `sizeof` 验证代码（自己写，不急 AI 代写），逐字段与今天手算对比，要求 100% 一致
- 接着做指针算术 / memcpy 反模式 / `#pragma pack(1)`（详见 W2D3 计划）

## 备注
- 自动计划曾把今天误标 W2D3（错位一天），已修正为 `plan-2026-09-08-W2D2.md`。
- 周会时间 = **10:00**（非 09:00），记牢。
- 红线：靶场只限自写程序 / Lyra / CrackMe / 公开样本静态 / CTF / SRC，禁止去任何在线游戏试。
