# 今日学习计划 · W2 第二天（2026-09-08 周二）

> 来源：游戏安全工程师培养计划 v3.2 + S1 执行方案 W2 章节（对齐/padding/指针算术）+ 周计划文件「下周任务-韦扬weiyang-20260906.md」
> 性质：**照着做**。验收看证据，不看感觉。
> 本日定位：**W2D2 手算日（不写代码）**。**注意：自动生成器误将本日标为 W2D3（plan-2026-09-08-W2D3.md），那是明天的写码验证活，今天不做。** 按周计划 + 实际进度（09-07 已完成 W2D1），今天 = 手算 3 个结构体。
> **AI 写码占比 ≤40% 红线前置声明**：今天**完全不写代码**，纯纸算。D3 起写 `offsetof` 验证代码也必须你自己写，导师只解释规则、定位"为何差了 N 字节"。

---

## 0. 今天一句话目标

**拿纸笔（或记事本）手算 3 个结构体的「每个成员偏移 + 总大小」，不碰编译器。** 把 W2D1 笔记里的对齐规则，从"看懂"变成"能一步步算对"。D3 才用 `offsetof`/`sizeof` 实测对比——顺序不能反（先手算、再验证）。

---

## 1. 主任务 · W2D2（手算 3 结构体）（~2.5 小时）

> 尺子回顾（你已懂）：x64 上每个成员对齐到自身类型大小（char 1 / short 2 / int 4 / 指针 8 / double 8）；数组按元素类型对齐；结构体总大小向上取整到**最大成员对齐值**的倍数；padding 是编译器插的空隙，内容未定义。

### 题目（3 个，难度递增，逐个手算）

**结构体 A**
```c
struct A { char a; int b; char c; };
```

**结构体 B**（与 A 同成员、换序，用来证明"换序大小变"）
```c
struct B { int b; char a; char c; };
```

**结构体 C**（复习昨天导师自纠的"104 是偏移不是总大小"）
```c
struct C { char buf[100]; double d; };
```

### 手算流程（严师铁律，4 步，一步一步写，禁止跳步）

1. **列对齐值**：把每个成员的 x64 对齐值写下来（char=1, int=4, double=8, 数组按元素）。
2. **从偏移 0 起，逐个算成员起始偏移** = 当前已用地址**向上取整到该成员对齐值**。
   - 例：已用到地址 1，下一个是 int（对齐 4）→ 起始偏移 = ceil(1,4) = 4（中间 [1,3] 是 padding）。
3. **算最后一个成员的结束地址** = 它的起始偏移 + 它自身占的字节数（**这一步最容易漏！导师 09-07 自己把"double 偏移 104"当成总大小，忘了 double 还占 8 字节到 112**）。
4. **总大小** = 结束地址**向上取整到最大成员对齐值的倍数**（不是最大成员体积的倍数）。

### 验收（手算稿存盘，D3 用 offsetof 对照）

- [ ] A 手算：a@? b@? c@? 总大小=? —— 预期：a@0, b@4, c@8, size=12
- [ ] B 手算：b@? a@? c@? 总大小=? —— 预期：b@0, a@4, c@5, size=8
- [ ] C 手算：buf@? d@? 总大小=? —— 预期：buf@0, d@104（**104 是 d 的偏移，不是总大小**）, size=112
- [ ] 能口述"**为什么 A(12) ≠ B(8) 但成员一模一样**"：padding 落在哪取决于前面排到哪，换序改变缝的位置 → 浪费量变
- [ ] 手算稿（拍照/截图/文字）存 `daily/2026-09-08.md` 或 `notes/`

> **注意 C 的教学点**：d 的偏移是 104，但 d 自己还占 8 字节（到 112），所以总大小是 **112 不是 104**。这正是 09-07 导师当场认错的那一处——你今天亲手算一遍，比听我讲十遍都牢。

---

## 2. 配套纪律（严师铁律）

- **先手算、再验证，顺序不能反**：今天不许开编译器。周会 / 未来抽问一问"为什么这个成员前面有 3 字节 padding"就露馅，靠跑代码蒙不过去。
- **AI 写码占比 ≤40%**：今天零代码，红线天然满足。D3 写码时也必须是你自己写主逻辑。
- **日报（≤3 分钟）**：收工发三行 + 三色灯。
  ```
  【D2 ｜ W2 ｜ 🟢/🟡/🔴】
  今天要跑通：手算 3 结构体(A/B/C)大小与成员偏移，不写码
  跑通了没：全算完 / 卡在 [具体一步]
  明天第一件事：写 offsetof/sizeof 验证代码，逐字段与今天手算对比
  ```

---

## 3. 今日必须落盘的东西（个人进度隔离）

| 产出类型 | 落盘路径（韦扬） |
|---|---|
| W2D2 日报 + 手算稿（A/B/C 偏移表 + 总大小） | `.workbuddy/memory/progress/韦扬/daily/2026-09-08.md` |
| （可选）手算过程笔记 | `.workbuddy/memory/progress/韦扬/notes/` |

- [ ] ✅ 手算稿（A/B/C 三张偏移表）→ `daily/2026-09-08.md`
- [ ] ✅ 日报三行 + 三色灯
- [ ] ⏸️ GitHub 仓库：今天纯手算不碰代码，无 push；若记了笔记想同步，可 `add/commit`（暂不强制 push）。`.workbuddy/` 含隐私，**绝不可 `git add .` 整目录**。

---

## 4. 明天第一件事（W2D2 → 周三 9/09 · W2D3 写码验证日）

- **W2D3 主事**：把今天手算的 A/B/C 写成 `offsetof`/`sizeof` 验证代码（你自己写），逐字段与手算对比，要求 **100% 一致**；然后做指针算术步长 + memcpy 反模式 + `#pragma pack(1)` 对照（详见自动生成的 `plan-2026-09-08-W2D3.md`，那份是 D3 的正确内容）。
- 若今天手算与 D3 实测不一致 → **先回头改手算稿、写归因**，再进 D3，不抢进度。

---

## 5. 常见坑

- ❌ 手算时把"成员偏移"当成"总大小"（导师 09-07 刚犯过，漏掉成员自身占的字节）。
- ❌ 忘记总大小要向上取整到**最大对齐值**（不是最大成员体积）的倍数。反例 C：max(sizeof)=100，max(align)=8，取整到 8 的倍数=112。
- ❌ 先开编译器跑出答案再"倒推"手算 → 这叫作弊，抽问必露馅。
- ❌ A 和 B 算出来一样大 → 说明 padding 规则没真懂，回去重看对齐值。

---

## 6. 严师一句话

W2 的刻度尺今天交到你手里——**手算是"用规则丈量内存"，不是"用眼睛猜大小"**。今天把 A/B/C 算到不看答案也能写对，D3 写码验证才会是"确认"而非"碰运气"，W3 虚表偏移、W6 多级指针你才站得稳。

---

> 红线提醒：靶场只限自写程序 / Lyra / CrackMe / 公开样本（静态）/ CTF / SRC 授权环境。**任何「去某在线游戏里试试」的想法，一律不做。** 今天只是指针与结构体，碰不到红线，但先刻进脑子。
