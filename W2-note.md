# W2 对齐 / padding / 指针算术 笔记（韦扬，已填定稿）

> 说明：D2 手算、D3 用 offsetof 验证后再回填实证。

## 1. 什么是对齐（自己话）
- 定义：每个成员变量存放的初始地址，必须是其"自身类型大小"的整数倍。
- 为什么编译器要插对齐（不插会怎样）：不插的话，单次读会变成"双次读+拼装"（性能裂变）；硬件按总线宽度取数，地址不对齐要分两次取再拼，某些架构直接 fault。

## 2. x64 各类型对齐值（先抄，但要懂）
char=1 / short=2 / int=4 / 指针=8 / double=8

## 3. 结构体总大小规则（自己话）
- 规则：总大小向上取整到 **最大对齐值** 的倍数。
- 最大成员大小 vs 最大对齐值：
  最大成员大小 = 结构体里单个成员占字节最多的那个的字节数（max(sizeof)）；
  最大对齐值 = 结构体里对齐要求最严的成员的对齐数（max(alignof)，x64 下 double/指针=8 最严）。
  两者常相等（只含基础类型时），但不总相等：struct { char buf[100]; double d; } 最大成员大小=100、最大对齐值=8。手算：buf[100] 占偏移 0..99；double 需对齐 8，偏移向上取整到 104（占 104..111，8 字节）；占用到 112，已是 8 的倍数，故总大小=112 而非 100。注意 104 是 double 的**偏移**、不是总大小（导师初版误把 104 当总大小，已纠）。
  各字段落在哪由它自己的对齐值决定；最大对齐值决定结构体自身的对齐，进而决定数组里下一个元素从哪开始（数组步长）。

## 4. padding 是什么（重点）
- 定义：编译器为了让下一个成员对齐，在俩成员之间硬塞的空闲字节。
- 内容是什么：未定义 / 是 0 吗？答：否（垃圾值，不是 0）。
- 为什么不能假设它是 0：padding 里是未初始化的任意垃圾值（不是 0）；把它当有效数据用（如 memcpy 成字节流比较/哈希/网络发送）会泄露内存残值，且结果每次不确定。头号危害是序列化/网络传输导致"信息泄露"安全漏洞。

## 5. 换顺序大小变（核心，周会必考）
- 例子 A {char; int; char} = 12 字节
- 例子 B {int; char; char} = 8 字节
- 为什么变：内部填充（padding）和尾部填充（trailing padding）的总量，取决于"高对齐字段（int）"的放置位置——排列顺序决定缝落在哪、浪费多少。

## 6. 指针算术
- p+1 实际 +sizeof(*ptr) 字节？取决于：指针类型。
- 举例 int* 和 double* 各 +几：int* +4，double* +8。

## 7. #pragma pack(1) 代价
- 它干了啥：强制 1 字节对齐、去掉 padding。
- 代价（非对齐访问）：x86 能跑但慢，某些架构直接崩。

## 8. 避坑（S3 用）
- 别把结构体 memcpy 成字节流按偏移取值，正确做法：逐字段读，别假设布局稳定。

---

## 专项 2：git 分支核对记录（细节核对清单）
- 核实方式 1（本地）：`git branch -r` → 显示 `origin/master`
- 核实方式 2（GitHub API）：`https://api.github.com/repos/weiyang-xxx/game-security-notes` → `"default_branch": "master"`
- 结论：本人仓库默认分支 = **master**，git 命令保持 `git push -u origin master`，**不改 main**
- 教训：任务文件里"GitHub 新仓默认 main"是针对 zl 的 generic 结论，不适用于本人；凡关键信息先实测再写。
- 周会时间更正：实际为 **10:00**（非 09:00），以后关键时间一律查证后写。
