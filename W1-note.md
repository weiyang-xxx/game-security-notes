# W1 内存四区 + 我踩的 ASLR 错觉

> 作者：韦扬 ｜ 2026-09-04 ｜ W1D6 第一周公开笔记（W12 硬门槛第 1/12 篇）

## 1. 我实测到了啥
- 递归探栈：每层 `&local` 递减 `0x130`（304 字节），栈向低地址增长。
- 5 次 `new` 地址不严格递增 → 堆不是无脑向上。
- `g_msg_ptr` 在 `.data`、它指的 `"hello"` 在 `.rdata`，两个地址分属两区。
- 本机实测量级：**栈≈825GB < 堆≈2.85TB < 模块≈140TB**。

## 2. 我原先以为
堆在下、栈在上——就是教科书那张内存布局图里画的：栈顶在高地址、堆在它下面。

## 3. 打脸时刻
看到实测数据那一刻我懵了：栈≈825GB 居然比堆≈2.85TB 还低，模块更是高到 140TB，跟教科书那张图完全反着。原来我之前对堆和栈的认知，是被那张 32 位老图带偏的。

> 注：本段由导师按「打脸」语义基于学员原话"推翻了我之前对堆和栈的认知"改写，学员可替换为自己的真实反应。

## 4. 为啥会这样
- 栈向下增长是 **x64 ABI 约定**，不是物理定律，别当铁律。
- ASLR 每次启动只随机化模块/栈/堆摆在内存的**绝对落点**，不碰指令里写死的**相对间隔**——所以"房间号"雷打不动。
- 教科书那张图是 32 位模型，在 Win x64 + ASLR 下根本不成立。

## 5. 这周收获
能大致读懂给出的代码是什么意思，并且能给代码加注释了。比背结论值钱的是：亲手用代码推翻了一张权威图。

---

（计划：本周末定稿后发 GitHub Pages 博客 `https://weiyang-xxx.github.io/game-security-notes/`）
