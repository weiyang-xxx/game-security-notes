【D1 ｜ W2 ｜ 🟢】

今天要跑通：①对齐原理啃完+笔记定稿 ②git 分支核对(master 确认) ③周会时间记牢(10:00) ④D1 日报

跑通了没：
- 笔记骨架 8 节全填定稿（progress/韦扬/notes/w2-align-note.md）；§3「最大成员大小 vs 最大对齐值」经 5 轮迭代最终采纳标准定义（反例 char[100]+double→112，104 为 double 偏移；导师初版误把 104 当总大小已纠），§4 信息泄露危害、§5 换序变机制均过。
- git 默认分支核实 = master（本地 git branch -r + GitHub API 双确认），命令保持 `git push -u origin master`，不改 main。
- 周会时间更正为 10:00，已记入笔记专项2。

卡在哪一步：§3 概念花了 5 轮才讲清（曾误把"最大成员大小"当主控对齐的角色，v4 还编了"char+int 在 Linux sizeof=16"的假例子），已纠。

明天第一件事：D2 手算 3 个结构体大小与成员偏移（先手算，不写码）；周会 10:00 抽问（画四区图 + 3 分钟口述 进程/模块/节区/IAT/PEB）。
