# 明日学习计划 · W2 第三天 / D3（2026-09-09 周三）

> 性质：**预生成，明天照着做**。验收看证据，不看感觉。
> 本日定位：**W2 写码验证日（D3）**。D1（09-07）啃原理+笔记骨架+git核对；D2（09-08）手算 A/B/C 偏移与大小（已收工 🟢）；**今天把 D2 手算用 `offsetof`/`sizeof` 实测一遍，要求 100% 一致**，再练指针算术 / memcpy 反模式 / `#pragma pack(1)`，给 W3 虚表偏移铺路。
> **AI 写码占比 ≤40% 红线前置声明**：今天三个验证程序（`verify.cpp` / `ptr_arith.cpp` / `memcpy_pitfall.cpp` / `pack_demo.cpp`）**全部你自己写**。导师只给骨架（含 `TODO(learner)`）和帮你定位"为什么手算和实测差了 N 字节"，**不能替你写主逻辑**。

---

## 0. 今天一句话目标

把 D2 的纸算变成代码实证：**手算与实测 100% 一致**（W2 验收标准①）；并搞清三件事——指针算术步长、`memcpy`+裸偏移是地雷、`#pragma pack(1)` 用安全换字节。为 W3 虚表偏移 / W6 多级指针打底。

**W2 收口轨迹**：D1 原理 → D2 手算 → **D3 写码验证+深化** → D4 缓冲+笔记+weekly 草稿 → D7 归档。

---

## 1. 主任务（~3.5 小时）

> 尺子回顾（你已懂）：x64 上 char1/short2/int4/指针8/double8 对齐；总大小取整到**最大成员对齐值**；padding 内容未定义；**指针算术步长 = `sizeof(指向类型)`**；`#pragma pack(1)` 取消 padding 但代价是非对齐访问。

### 任务 A · 验证 D2 手算（核心，W2 验收①）（~60 min）
1. **写 `verify.cpp`**：把 D2 的 A/B/C 三个结构体用 `offsetof(S, 成员)` 和 `sizeof(S)` 把每个成员偏移 + 总大小打出来。
   - A `{char a; int b; char c;}` → 预期 a@0 b@4 c@8 size=12
   - B `{int b; char a; char c;}` → 预期 b@0 a@4 c@5 size=8
   - C `{char buf[100]; double d;}` → 预期 buf@0 d@104 size=112
2. **逐字段跟 D2 手算稿对比**，必须 100% 一致。不一致就定位差在哪（最常见：把 d@104 当总大小 → 漏了 double 自己占的 8 字节）。
3. **验收**：`verify.cpp` 输出截图 + 一张"手算 vs 实测"对照表存 `daily/2026-09-09.md`。

### 任务 B · 指针算术步长精练（~70 min）
1. 手算（先手算再跑）：取 `struct { double d; char* p; int i; }`（x64，总大小 **24**，padding 在字节 **[20,23]**）：
   - `&s.d`=base+0；`&s.p`=base+8；`&s.i`=base+16；结束=base+24。
2. **写 `ptr_arith.cpp`**：
   - 用 `(char*)&s + offsetof(S, x)` 算各成员地址，与 `&s.x` 直接比对（应完全相等）；
   - 演示指针算术：`int* ip = &s.i; ip + 1` 的地址 = `&s.i + sizeof(int)` = +4，打印差值证明**步长=`sizeof(指向类型)` 而非 1**；
   - 画"指针+N 跳到哪"对照：+0→d, +8→p, +16→i, +20→(pad), +24→end。
3. **验收**：输出截图（地址逐字段相等、步长=4）+ 能口述"**`p+1` 差 `sizeof(int)` 不是 1，因为指针算术以指向类型为单位**"。

### 任务 C · memcpy 反模式实战（W2 头号坑）（~90 min）
1. **写 `memcpy_pitfall.cpp`**：取 `struct { char a; int b; short c; }`（size **12**，padding 在 [1,3] 与 [10,11]）：
   - `memcpy` 进 `char buf[sizeof(S)]`，再用裸偏移 `*(int*)(buf + offsetof(S, b))` 读 `b`，与 `s.b` 比对（**本地会一致——这正是陷阱**）；
   - 再写正确的 `serialize_fields`/`deserialize_fields`：**逐字段**写/读（显式把 a/b/c 写到 buf 固定位置，不依赖 struct 内部 padding）。
2. **写归因文字（排错病历）**：为什么 memcpy+裸偏移是地雷——struct 内部 padding 字节内容未定义（Debug 填 0xCC / Release 随机），一旦跨编译器/平台/结构体改动，裸偏移读出的字段就错位；逐字段读写不依赖布局，稳健。这条到 **S3 的 `FTransform` 实测会救命**（UE 坐标变换结构体绝不能赌布局）。
3. **验收**：输出截图（裸偏移读一致 + 逐字段读一致）+ 归因文字存 `daily/2026-09-09.md` 或 `notes/`。

### 任务 D · `#pragma pack(1)` 对照（~25 min）
1. **写 `pack_demo.cpp`**：`#pragma pack(1) struct Packed { char a; int b; short c; }` → 手算预期 `a@0, b@1, c@5, size 7`（**无 padding**）。跑 `offsetof` 验证。
2. **解释代价**：非对齐访问在某些架构直接崩 / 变慢；x64 上读未对齐成员可能触发额外总线周期。知道代价，别为"省几字节"滥用。
3. **验收**：输出截图（a@0,b@1,c@5,size 7）+ 一句解释。

### 抽问 drill（自问自答）
- **题**："S3 `{ double d; char* p; int i; }` 24 字节，指出 padding 在哪两个字节区间，为什么。"
- **答（自己写一遍再对）**：padding 在字节 **[20,23]**（4 字节）。`i` 在 offset 16 占 4 → 到 19；总大小须取整到最大对齐值 8 → 20 不是 8 倍数，补到 24，pad 20–23。
- 文字存档 `daily/2026-09-09.md`。

---

## 2. 配套纪律（严师铁律）

- **顺序铁律**：先手算（D2 已做）→ 再写码验证（今天）。别先跑代码再反推，周会抽问"为什么这个成员前有 3 字节 padding"会露馅。
- **AI 写码 ≤40% 红线**：四个 cpp 你亲自写；我只能解释规则、定位"差了 N 字节"的 bug，**不替你写主逻辑**。
- **日报（≤3 分钟）**：
  ```
  【D3 ｜ W2 ｜ 🟢/🟡/🔴】
  今天要跑通：offsetof 验证 D2 手算(100%一致) + 指针算术 + memcpy反模式 + pack(1) + 24字节抽问
  跑通了没：全过 / 卡在 [具体一步]
  明天第一件事：写 W2 公开笔记 + 起 W2 weekly.md 草稿 + 缓冲回锅 D1–D3 裂缝
  ```

---

## 3. 今日必须落盘的东西（个人进度隔离）

**隔离硬规则**：本共享日计划 `plan-*.md` 留 `memory/` 根，两人同看，**不算个人进度**。你（韦扬，learner #1）的每一项个人产出**必须**存自己独立目录，禁止跨成员混写/合并/覆盖。

| 产出 | 落盘路径（韦扬） |
|---|---|
| D3 日报 + 验收证据（手算vs实测对照表 / 各 cpp 输出截图 / memcpy 归因 / 抽问答案） | `progress/韦扬/daily/2026-09-09.md` |
| W2 公开笔记初稿（对齐/padding/指针算术 + memcpy 反模式教训，本周 ≥1 篇） | `progress/韦扬/notes/` |
| W2 动手源码 `verify.cpp`/`ptr_arith.cpp`/`memcpy_pitfall.cpp`/`pack_demo.cpp` | 仓库 `code/02-align-padding/`（GitHub 公开） |

- [ ] ✅ `verify.cpp`：A/B/C 偏移+大小与 D2 手算 100% 一致（对照表）
- [ ] ✅ `ptr_arith.cpp`：地址逐字段相等 + 步长=`sizeof(int)`=4（截图）
- [ ] ✅ `memcpy_pitfall.cpp`：裸偏移读一致 + 逐字段读一致（截图）+ 归因文字
- [ ] ✅ `pack_demo.cpp`：a@0,b@1,c@5,size 7（截图）+ 一句解释
- [ ] ✅ 抽问答案（24 字节 padding 在 [20,23]）文字存档
- [ ] ✅ 日报三行 + 三色灯
- [ ] ⏸️ GitHub：今天动了代码/笔记记得 `add/commit/push` 保持连续链（仓库已 Public）。**绝不可 `git add .` 整目录**——`.gitignore` 已挡 `.workbuddy/`、`*.zip`、`*.exe`、`*.obj`。

---

## 4. 明天第一件事（W2D4 缓冲/收口日）
- 写 W2 公开笔记（优先发博客 `https://weiyang-xxx.github.io/game-security-notes/`）→ 起 W2 `weekly.md` 草稿（契约 7.9）→ 回锅 D1–D3 任何裂缝。
- 若 D1–D3 有未过验收（手算与实测不一致 / 抽问答不出）→ **先补齐再进笔记**，不抢进度。

---

## 5. 常见坑
- ❌ 把 `p+1` 当成地址 +1（实际 +`sizeof(指向类型)`）。`(char*)p+1` 才是地址 +1，`p+1` 跳过整个对象。
- ❌ memcpy 后"本地读出来一致"就以为安全 → 本地一致 ≠ 跨编译器/跨平台一致（padding 内容未定义）。正确姿势是**逐字段读写**。
- ❌ 先跑代码再手算 → 顺序反了，抽问答不出。先手算、再验证。
- ❌ `#pragma pack(1)` 后访问成员触发非对齐访问 → 某些架构直接崩。知道代价，别滥用。
- ⚠️ 全程 x64（double/指针对齐 8）；32 位对齐值不同，手算前先确认目标。
- ⚠️ `offsetof` 用标准宏，别手写 `(size_t)&((S*)0)->member` 之类 UB 写法。

---

## 6. 严师一句话
W2 的刻度尺你已经拿到手，今天把它磨利：**指针算术是"按类型丈量内存"、memcpy+裸偏移是"赌布局不变"、pack(1) 是"用安全换字节"**。这三样搞清楚，W3 虚表偏移、W6 多级指针、W9 PE 节表你才不会在字节级别栽跟头。连续链从 D1 第一条日报起算，绿格别断。

> 红线（刻着）：靶场只限自写程序 / Lyra / CrackMe / 公开样本静态 / CTF / SRC，**禁止去任何在线游戏试**。今天只是指针与结构体，碰不到红线，但先刻进脑子。
