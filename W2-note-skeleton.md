# W2 对齐 / padding / 指针算术 笔记骨架（韦扬，自己话填）

> 说明：本节是骨架，____ 处用自己的话写，别抄导师原句。D2 手算、D3 用 offsetof 验证后再回填实证。

## 1. 什么是对齐（自己话）
- 定义：____
- 为什么编译器要插对齐（不插会怎样）：____

## 2. x64 各类型对齐值（先抄，但要懂）
char=1 / short=2 / int=4 / 指针=8 / double=8

## 3. 结构体总大小规则（自己话）
- 规则：总大小向上取整到 ______ 的倍数
- 注意：是"最大对齐值"不是"最大成员大小"，区别是：____

## 4. padding 是什么（重点）
- 定义：____
- 内容是什么：未定义 / 是 0 吗？答：____
- 为什么不能假设它是 0：____

## 5. 换顺序大小变（核心，周会必考）
- 例子 A {char; int; char} = ____ 字节
- 例子 B {int; char; char} = ____ 字节
- 为什么变：____

## 6. 指针算术
- p+1 实际 +____ 字节？取决于：____
- 举例 int* 和 double* 各 +几：____

## 7. #pragma pack(1) 代价
- 它干了啥：____
- 代价（非对齐访问）：____

## 8. 避坑（S3 用）
- 别把结构体 memcpy 成字节流按偏移取值，正确做法：____

---

## 专项 2：git 分支核对记录（细节核对清单）
- 核实方式 1（本地）：`git branch -r` → 显示 `origin/master`
- 核实方式 2（GitHub API）：`https://api.github.com/repos/weiyang-xxx/game-security-notes` → `"default_branch": "master"`
- 结论：本人仓库默认分支 = **master**，git 命令保持 `git push -u origin master`，**不改 main**
- 教训：任务文件里"GitHub 新仓默认 main"是针对 zl 的 generic 结论，不适用于本人；凡关键信息先实测再写。
- 周会时间更正：实际为 **10:00**（非 09:00），以后关键时间一律查证后写。
